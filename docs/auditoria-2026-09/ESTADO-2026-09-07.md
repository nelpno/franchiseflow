# Estado da auditoria em 07/09/2026 — o que foi feito e o que falta

Sessão de 07/09/2026. **Ondas 0 a 3 concluídas e em produção. Onda 4 não começou.**

Commits: `bc52a1a` (onda 0) · `fce1eb2` (onda 1) · `141019f` (onda 2) · `c02638b` (onda 3) ·
`67053e4` (CLAUDE.md). Todos em `main`, empurrados e com o serviço atualizado no Portainer.

---

## Onda 0 — infra, RPCs, cron e segurança ✅

| O quê | Antes | Depois | Como foi verificado |
|---|---|---|---|
| Compressão e cache em produção | 2.226.597 B, **0% gzip**, sem `Cache-Control` | **248.411 B**, 100% gzip, `/assets` imutável 1 ano, HTML `no-store` | medido no live com stream cru (`fetch` descomprime sozinho e mente) |
| `export` + `recharts` no boot | 1,27 MB estáticos no chunk de entrada | fora do caminho crítico | `modulepreload` do `index.html` só cita vendor/supabase/dates/ui |
| `get_bot_conversation_summary` | 12.614 ms (estourava o timeout de 8 s) | ~66 ms | `EXPLAIN` + `pg_stat_statements` |
| RPC que falha | card vazio, silêncio | toast de erro | helper `rpc()` no `AdminDashboard` |
| `get_human_message_counts` | 1 linha por conversa, **truncado em 1.000** pelo PostgREST | `get_human_message_totals`, 62 linhas | contagem de linhas |
| Cron `aggregate_daily_data` | quebrado por overflow; série parada em 05/09 | `numeric(8,2)` + `LEAST` | 25/07, 29/07 e 06/09 reprocessados |
| Franqueado virar admin | **passava** | bloqueado | exploit re-executado; 3 ataques barrados, 5 fluxos legítimos intactos |
| Tela branca | sem guarda | `npm run lint:undef` + `verify:undef` | canário reprova, `src/` passa |

Dias recuperados no `daily_summaries`: 25/07 (R$ 17.054,86), 29/07 (R$ 9.824,02), 06/09 (R$ 16.742,60).

## Onda 1 — franqueado ✅

- **"Robô ativo" agora significa que ele conversou** (`get_franchise_bot_pulse`). 8 franquias que
  vendem com o robô mudo há 7+ dias viam "Tudo em dia!". Cenário `bot_parado` no `PriorityAction`.
- **Faixa do pedido em aberto** (`OpenOrderStrip`) — zero query nova; a home já baixava e descartava.
- **Frete em chips** a partir da tabela da própria franquia (`lib/deliveryFeeRules.js` + 20 testes
  com JSON real). Não adivinha valor: só preenche sozinho com uma opção única.
- **Comprovante logo após salvar** — o `saleId` do RPC era descartado.
- **Botão final do Meu Vendedor** deixou de nascer desabilitado; virou "Concluir".
- **Zeros falsos** antes do skeleton e **tour indevido** em celular novo (57 unidades) corrigidos.
- `DailyChecklist` saiu do load da home (tabela com zero linhas na vida, 576 req/dia).

## Onda 2 — admin ✅

- **Mensalidades**: `lib/subscriptionStatus.js` (+20 testes). "Sem cobrança" em vermelho para
  unidade sem linha; "Vencido há N dias" para PENDING já vencida (22 casos); chips que filtram;
  coluna de vencimento e valor; ordem por urgência de cobrança.
- **Financeiro**: coluna "vs mês ant." e ordenação "Maior queda". Comparativo honesto — no mês
  corrente o anterior é cortado no mesmo dia.
- **CS**: dono e WhatsApp no drawer, via `get_cs_franchise_contacts` (RPC separada de propósito).

## Onda 3 — vigilância ✅

- **Sentinela diária** (`pg_cron` jobid 5, 08:10 BRT): cron falho · `daily_summaries` sem o dia
  anterior · franquia que vende todo dia e parou · marketing duplicado · unidade sem cobrança ·
  pedido sem frete e sem justificativa. Escreve no sino do admin.
- **Superfície de ataque**: 50 → 21 funções `SECURITY DEFINER` expostas a `anon`, zero perigosas.

### Ações tomadas sobre o que a sentinela achou

- **R$ 1.200 de despesa duplicada removidos** (decisão do Nelson: "se é duplicada tira, já é
  lançado automático"). Saíram os lançamentos **manuais**; ficaram os automáticos, que têm
  `source_id` e reconciliam com `marketing_payments`:
  Cajamar 01/07 R$ 600 · Cotia 06/04 R$ 200 · Santana de Parnaíba 04/08 R$ 400.
  Backup: `public._backup_expenses_dup_marketing_2026_09_07` (3 linhas).
- **Rio Claro, pedido de 01/09 com frete zero**: legítimo — a entrega saiu junto com a de Limeira,
  na mesma viagem. Ficou registrado na observação do pedido, e a regra passou a ignorar frete zero
  **com justificativa escrita**. Convenção que fica: frete zero + observação preenchida = decisão
  consciente; sem observação = esqueceram.

### 🟡 Em aberto (decisão do Nelson)

- **Uberaba está ativa, com CPF, e sem nenhuma assinatura criada.** O cron de sync nunca a verá.
  Nelson vai criar depois. Até lá a sentinela vai apontar isso todo dia — é o comportamento certo.

---

## Onda 4 — o que falta (não começou)

Em ordem de (impacto × alcance) ÷ esforço. Detalhe completo em
[RELATORIO-CONSOLIDADO.md](RELATORIO-CONSOLIDADO.md) e nos 6 relatórios de frente.

| # | Item | Por quê | Esforço |
|---|---|---|---|
| 1 | **`(select fn())` nas ~100 policies restantes** | os helpers rodam **uma vez por linha**. Medido: `sales` na tela de Vendas com média de **1.090 ms**, `sale_items` 795 ms, contatos 3.458 ms; o `EXPLAIN` cai de 372 ms para **5,7 ms** só com a reescrita. Já aplicado em 3 policies (bot); faltam as outras | M |
| 2 | **Subset da icon-font** | Material Symbols variável = **1.128.840 bytes** baixados no boot, e até chegar cada ícone aparece como a palavra (`wb_sunny`). O app usa **141 nomes** → ≤ 30 KB | M |
| 3 | **Atribuição de marketing por unidade** | `contacts.ctwa_clid` em 69% dos contatos de 30 d; cruzado com vendas dá **33% da receita (R$ 115.927)**. Osasco ROAS 11,7 × Santos 0,2. **Zero telas leem** | M |
| 4 | **Filtro `franchise_id` explícito em Vendas e Gestão** | sem ele a RLS vira Seq Scan; com filtro as mesmas tabelas respondem em 9–71 ms | P |
| 5 | **Tap targets < 48 px e tipografia de 12 px** | bottom nav ~40 px, ações da venda 32 px, botões do estoque mobile **28 px**; `text-xs` é o tamanho mais usado do app (464×) e o DRE imprime em 11 px | M |
| 6 | **Extrair `lib/saleCalc.js` do `SaleForm` + testes** | subtotal, desconto, taxa repassada×absorvida e `netValue` vivem soltos no JSX, sem teste nenhum | M |
| 7 | **Tokens de cor por codemod** | 45% dos `className` com hex cru, 114 hex distintos (~45 são variação acidental); as variáveis CSS existem e têm **zero usos** fora de `ui/` | M |
| 8 | **Defaults de `ui/`** | `<Card>` tem a borda sobrescrita em 44 de 58 usos → **62 cards à mão contra 58 `<Card>`** | P→M |
| 9 | **`EmptyState`/`ErrorState` compartilhados** | `FranchiseeDashboard` sem estado vazio; `TabResultado` sem UI de erro | P |
| 10 | **Aba "Fechamento" no Financeiro** | fechar o mês exige 3 telas que não se cruzam | M |
| 11 | **Um sino só + poll de 5 min** | o Layout monta **dois** `NotificationBell` de 2 min → `notifications` é **23% de todo o tráfego** (6.021 req/dia), 82% "Estoque baixo", 59% nunca lidas | P |
| 12 | **`TabResultado` com janela de data** | carrega o histórico inteiro; pior caso Vila Maria = 1.069 vendas / 2.434 itens, 31 requests, ~1 MB | M |
| 13 | **Apagar o morto** | `MyChecklist` + `daily_checklists`, 6 RPCs, 7 tabelas de backup, índice de 17 MB nunca lido, 4 funções apontando para workflow inativo | P |
| 14 | **react-query fase A** | `useFranchises` (chamado em quase toda tela) e depois Vendas e Início | M |

**Não fazer** (já decidido): virtualizar TabEstoque/TabLancar (máx. 56 itens), mexer no `index.css`
(Tailwind legítimo, 17 KB gzip), remover react-query, recriar Health Score / Acompanhamento /
Relatórios / BotIntelligence.

---

## Armadilhas para a próxima sessão

1. 🔴 **Não dar `CREATE OR REPLACE` em `get_franchise_health_signals` nem em
   `reconcile_cs_auto_tasks`.** As versões que rodam em produção têm regras (`giro_baixo`,
   `marketing_late`, `cs_agreements`, cooldown, `parked_until`) que **não estão em `.sql` nenhum**
   do ecossistema. O `09-*.sql` do repo é mais velho e regride o radar do Celso.
2. **Rodar `npm run lint:undef` antes de qualquer deploy.** Ela já pegou um erro real (um
   `formatBRL` não importado) que passava no build e no lint normal e deixaria o Financeiro branco.
3. **`git push` puro trava** (GCM headless no Windows). Usar o push por URL com token — e lembrar
   que ele **não atualiza `origin/main`**: provar com `git ls-remote origin main`.
4. **Verificar deploy por CONTEÚDO**, não por hash (o build da VPS às vezes difere do local).
   Script pronto: `.tmp/audit-2026-09/verify-onda1.mjs` (troque a lista de strings).
5. **Regra de alarme nasce errada com facilidade.** A de marketing duplicado acusou 3 franquias
   corretas na primeira execução. Rodar dentro de um `DO` que termina em `raise exception` para ver
   o resultado sem gravar notificação.
6. **Clarity: 10 requisições por dia.** Já foram 2 nesta sessão (07/09). O resumo agregado está em
   `00b-CLARITY-3DIAS.md`.
